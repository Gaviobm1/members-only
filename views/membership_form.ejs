<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/header.ejs") %>
  <body>
    <%- include("./partials/nav.ejs") %> <% if (user.membership_status) { %>
    <a href="<%= user.url %>">Already a member!</a>
    <% } else if (typeof user !== "undefined" || !user.membership_status) {%>
    <form action="" method="POST">
      <label for="secret">Secret Password</label>
      <input type="text" name="secret" id="secret" required />
      <button>Become a member</button>
    </form>
    <% } else { %>
    <p>Membership is for users who sign up only!</p>
    <% } %> <%- include("./partials/footer.ejs") %>
  </body>
</html>
